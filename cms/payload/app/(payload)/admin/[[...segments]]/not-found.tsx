/**
 * Payload Admin Panel — Not Found Page
 *
 * Rendered by Next.js when a navigation within the admin panel resolves
 * to a route that does not exist. Delegates to Payload's built-in
 * `NotFoundPage` component which renders a consistent 404 UI.
 */
import { NotFoundPage } from '@payloadcms/next/views'

export default NotFoundPage
